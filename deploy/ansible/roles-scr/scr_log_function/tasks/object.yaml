---
# scr_log_function/tasks/object.yaml
#
# CRUD operations on Ansible facts serialised as JSON blobs.
# Each fact is stored as a separate blob in the objectstore/ prefix.
#
# Supported actions: store | retrieve | update | delete
#
# vars:
#   payload:  fact name (string) or list of fact names
#   result_var:       variable name to register retrieved data (retrieve only)
#
# Blob path: scr-runs/<run_id>/objectstore/<fact_name>.json

#           single  multi  Complex
# Store       ✔       ✔     ✔
# Update      ✔       ✔     ✔
# Retrieve    ✔       ✔     ✔
# Delete      ✔       ✔     ✔
#
# where:
#   single  = payload is a string representing a single fact name (e.g. "my_fact")
#   multi   = payload is a list of strings representing multiple fact names (e.g. ["fact_one", "fact_two"])
#   complex = a dictionary of dictionaries and lists
#
# Notes:
#   - store: create or overwrite a blob with the provided fact(s)
#   - update: merge provided fact(s) with existing blob (new keys are added, existing keys are updated, keys not in payload are left unchanged)
#   - retrieve: read blob and promote specified keys to facts
#   - delete: remove specified keys from blob (if multi or complex) or delete entire blob (if single)


# - name:                                 "SCR Log Function [object/{{ action }}] -----DEBUGGING-----"
#   ansible.builtin.debug:
#     msg: |
#                                         payload:      {{ payload      | default('undefined') }}
#                                         action:               {{ action               | default('undefined') }}
#                                         _object_store:        {{ _object_store        | default('undefined') }}
#                                         _payload_names:       {{ _payload_names       | default('undefined') }}
#                                         _payload_updates:     {{ _payload_updates     | default('undefined') }}
#     verbosity:                          0
#   delegate_to:                          localhost
#   run_once:                             true

# /*---------------------------------------------------------------------------8
# | INITIALIZE                                                                 |
# +------------------------------------4---------------------------------------*/
# region: OPERATION: ALL
- name:                                 "SCR Log Function [object/{{ action }}] - Normalize payload into a list (string OR list)"
  ansible.builtin.set_fact:
    _payload_names: >-
                                        {{
                                          payload
                                          if (payload is sequence and payload is not string)
                                          else [payload]
                                        }}
  delegate_to:                          localhost
  tags:
    - silent_v1
# endregion

# /*---------------------------------------------------------------------------8
# |                                                                            |
# |  DOWNLOAD — download OBJECT_STORE blob and load JSON into a _object_store  |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
# region: OPERATION: ALL
- name:
  block:

# Download the OBJECT_STORE to the localhost server.
#
# Note: we attempt to download the blob every time to ensure we have the latest version of the data,
#       especially for update operations where we merge new data with existing data. If the blob does
#       not exist (e.g. first time store operation), we handle the error and continue with an empty
#       object store.
#
# Note: The 'az' commands are only available on the localhost where the SCR Log Function role is executed, 
#       which is why we delegate this task to localhost. The remote hosts may not have the Azure CLI 
#       installed,and even if they do, it is more efficient to perform the download once on the localhost
#       and then distribute the data as needed, rather than having each remote host attempt to download the
#       same blob from Azure Storage.
    - name:                                 "SCR Log Function [object/{{ action }}] - Download OBJECT_STORE JSON from Azure"
      ansible.builtin.command: >-
                                            az storage blob download
                                              --account-name   {{ scr_azure_storage_account }}
                                              --container-name {{ scr_azure_container }}
                                              --name           {{ scr_log_function_remote_objectstore }}
                                              --file           {{ scr_log_function_local_objectstore }}
                                              --overwrite
                                              --auth-mode      login
      delegate_to:                          localhost
      changed_when:                         false
      register:                             blob_download
      failed_when:                      
                                            - blob_download is false
                                            - "'BlobNotFound' not in (blob_download.stderr | default(''))"

# Delegate to localhost to read the downloaded JSON file and load it into a fact called _object_store for use
# in subsequent tasks. If the file does not exist (e.g. blob was not found), we default to an empty dictionary.
#
# Note: _object_store will be set in the current host context.
    - name:                                 "SCR Log Function [object/{{ action }}] - Load JSON into _object_store "
      ansible.builtin.set_fact:
        _object_store:                      "{{ lookup('file',
                                                        scr_log_function_local_objectstore,
                                                        errors='ignore')
                                                | default('{}', true) 
                                                | from_json }}"
      delegate_to:                          localhost

    - name:                             "SCR Log Function [object/{{ action }}] - Remove downloaded OBJECT_STORE JSON file (delete file)"
      ansible.builtin.file:
        path:                           "{{ scr_log_function_local_objectstore }}"
        state:                          absent
      delegate_to:                      localhost

  tags:
    - silent_v1

# endregion

# /*---------------------------------------------------------------------------8
# |                                                                            |
# |  RETRIEVE — Promote specific JSON key to fact                              |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
# region: OPERATION: retrieve
- name:                                 "SCR Log Function [object/{{ action }}] - Promote specific JSON key to fact"
  block:

# Each item in _payload_names can be either:
#   "fact_name"               → sets fact on current host
#   "fact_name, hostname"     → sets fact on the localhost via delegate_facts
# Note: we use delegate_facts to set facts on the localhost when the payload item specifies 'localhost'
#       as the host, which allows us to store and retrieve data that is specific to the localhost context
#       (e.g. file_store) while still using the same mechanism for promoting data from the object store to
#       facts, regardless of whether the data is intended for the localhost or remote hosts. This approach
#       provides flexibility in how we manage and access data across different hosts while maintaining a
#       consistent interface for interacting with the object store.
    - name:                             "SCR Log Function [object/{{ action }}/({{ _payload_names | map('regex_replace', '^\\s*([^,]+).*$','\\1') | map('trim') | list }})] - Promote specific JSON key to fact"
      ansible.builtin.set_fact:
        "{{ _fact_name }}":             "{{ _object_store[_fact_name] }}"
      loop:                             "{{ _payload_names }}"
      loop_control:
        loop_var:                       _obj_loop_item
      # delegate_to:                      "{{ _fact_host if _fact_host else 'localhost' }}"
      # delegate_facts:                   "{{ true if _fact_host else false }}"
      delegate_to:                      "{{ _fact_host }}"
      delegate_facts:                   "{{ true if _fact_host == 'localhost' else false }}"
      vars:
        _fact_parts:                    "{{ _obj_loop_item.split(',') }}"
        _fact_name:                     "{{ _fact_parts[0] | trim }}"
        _fact_host:                     "{{ 'localhost'
                                              if (_fact_parts[1] | default('') | trim) == 'localhost'
                                              else inventory_hostname
                                        }}"
      when:                             _fact_name in _object_store

  when:                                 action == "retrieve"
  tags:
    - silent_v1
# endregion


# /*---------------------------------------------------------------------------8
# |                                                                            |
# |  STORE / UPDATE — serialize fact(s) to JSON and upload                     |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
# region: OPERATION: store/update
- name:                                 "SCR Log Function [object/{{ action }}] - Upsert fact(s) into object store (merge with existing blob if update, otherwise overwrite)"
  block:

    # Each item in _payload_names can be either:
    #   "fact_name"               → resolves from current vars (hostvars of current host)
    #   "fact_name, localhost"    → resolves from hostvars['localhost'][fact_name]
    - name:                             "SCR Log Function [object/{{ action }}/({{ _payload_names | map('regex_replace', '^\\s*([^,]+).*$','\\1') | map('trim') | list }})] - Build update object from the named facts"
      ansible.builtin.set_fact:
        _payload_updates:               "{{ _payload_updates | default({})
                                            | combine({
                                                (_fact_name):
                                                  hostvars[_fact_host][_fact_name]
                                                  if _fact_host == 'localhost'
                                                  else vars[_fact_name]
                                              }) }}"
      loop:                             "{{ _payload_names }}"
      loop_control:
        loop_var:                       _obj_loop_item
      delegate_to:                      localhost
      vars:
        _fact_parts:                    "{{ _obj_loop_item.split(',') }}"
        _fact_name:                     "{{ _fact_parts[0] | trim }}"
        _fact_host:                     "{{ 'localhost'
                                              if (_fact_parts[1] | default('') | trim) == 'localhost'
                                              else inventory_hostname
                                        }}"
      when:                             (_fact_host == '' and vars[_fact_name] is defined)
                                        or
                                        (_fact_host != '' and hostvars[_fact_host][_fact_name] is defined)


    - name:                             "SCR Log Function [object/{{ action }}] - Merge existing + updates (updates win on key collisions)"
      ansible.builtin.set_fact:
        _object_store:                  "{{ _object_store | combine(_payload_updates, recursive=false) }}"
      delegate_to:                      localhost

  when:                                 action in ['store', 'update']
  tags:
    - silent_v1
# endregion


# /*---------------------------------------------------------------------------8
# |                                                                            |
# |  DELETE — remove blob(s) from Azure                                        |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
# region: OPERATION: delete
- name:                                 "SCR Log Function [object/{{ action }}] - Remove one key from payload dict"
  block:

    - name:                             "SCR Log Function [object/{{ action }}/({{ _payload_names | map('regex_replace', '^\\s*([^,]+).*$','\\1') | map('trim') | list }})] - Remove one key from payload dict"
      ansible.builtin.set_fact:
        _object_store:                  "{{ _object_store
                                            | dict2items
                                            | rejectattr('key', 'eq', _fact_name)
                                            | items2dict }}"
      loop:                             "{{ _payload_names }}"
      loop_control:
        loop_var:                       _obj_loop_item
      delegate_to:                      localhost
      vars:
        _fact_parts:                    "{{ _obj_loop_item.split(',') }}"
        _fact_name:                     "{{ _fact_parts[0] | trim }}"
      changed_when:                     true

  when:                                 action == "delete"
  tags:
    - silent_v1
# endregion


# /*---------------------------------------------------------------------------8
# |                                                                            |
# |  WRITE OBJECT_STORE — Write updated object store to file and upload to     |
# |                       Azure                                                |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
# region: OPERATION: store/update/delete
- name:                                 "SCR Log Function [object/{{ action }}] - Write updated object store to file and upload to Azure"
  block:

    - name:                             "SCR Log Function [object/{{ action }}] - Write fact to JSON file"
      ansible.builtin.copy:
        dest:                           "{{ scr_log_function_local_objectstore }}"
        content:                        "{{ _object_store | to_nice_json(indent=2, sort_keys=True) }}\n"
        mode:                           "0644"
      delegate_to:                      localhost

    - name:                             "SCR Log Function [object/{{ action }}] - Upload JSON file to Azure"
      ansible.builtin.command: >-
                                        az storage blob upload
                                          --account-name   {{ scr_azure_storage_account }}
                                          --container-name {{ scr_azure_container }}
                                          --name           {{ scr_log_function_remote_objectstore }}
                                          --file           {{ scr_log_function_local_objectstore }}
                                          --overwrite
                                          --auth-mode     login
      delegate_to:                      localhost
      changed_when:                     true

    - name:                             "SCR Log Function [object/{{ action }}] - Remove file (delete file)"
      ansible.builtin.file:
        path:                           "{{ scr_log_function_local_objectstore }}"
        state:                          absent
      delegate_to:                      localhost

  when:                                 action in ['store', 'update', 'delete']
  tags:
    - silent_v1
# endregion


# /*---------------------------------------------------------------------------8
# |                                                                            |
# |  CLEANUP                                                                   |
# |                                                                            |
# +------------------------------------4--------------------------------------*/
# region: OPERATION: ALL
- name:                                 "SCR Log Function [object/cleanup] - Cleanup temporary files and reset facts"
  block:

    - name:                             "SCR Log Function [object/cleanup] - Set facts to null"
      ansible.builtin.set_fact:
        "{{ _obj_loop_item }}":                   null
      loop:
                                        - _payload_names
                                        - _payload_updates
                                        - _object_store
      loop_control:
        loop_var:                       _obj_loop_item  
      delegate_to:                      localhost
      changed_when:                     true

  tags:
    - silent_v1
# endregion
